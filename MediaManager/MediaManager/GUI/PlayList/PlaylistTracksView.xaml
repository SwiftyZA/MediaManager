<UserControl x:Class="MediaManager.GUI.PlayList.PlaylistTracksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:cal="http://www.caliburnproject.org">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <TreeView x:Name="Tracks" ItemsSource="{Binding Tracks}" Margin="5" Width="400" Height="400" >
        </TreeView>
        <Button Grid.Row="1" x:Name="TryClose" Content="Close" HorizontalAlignment="Right" Margin="5" Width="100"/>
    </Grid>
</UserControl>
